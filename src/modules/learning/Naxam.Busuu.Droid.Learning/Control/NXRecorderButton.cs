using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

using Android.App;
using Android.Content;
using Android.OS;
using Android.Runtime;
using Android.Util;
using Android.Views;
using Android.Widget;

namespace Naxam.Busuu.Droid.Learning.Control
{
    public class NXRecorderButton : RelativeLayout
    {
        public NXRecorderButton(Context context) : base(context)
        {
        }

        public NXRecorderButton(Context context, IAttributeSet attrs) : base(context, attrs)
        {
        }

        public NXRecorderButton(Context context, IAttributeSet attrs, int defStyleAttr) : base(context, attrs, defStyleAttr)
        {
        }

        public NXRecorderButton(Context context, IAttributeSet attrs, int defStyleAttr, int defStyleRes) : base(context, attrs, defStyleAttr, defStyleRes)
        {
        }

        protected NXRecorderButton(IntPtr javaReference, JniHandleOwnership transfer) : base(javaReference, transfer)
        {
        }
    }
}