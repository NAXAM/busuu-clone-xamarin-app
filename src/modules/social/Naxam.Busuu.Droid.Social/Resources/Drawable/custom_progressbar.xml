<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="#F1F5F6"
                android:centerColor="#F1F5F6"
                android:centerY="1.0"
                android:endColor="#F1F5F6"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="#D5EFFB"
                    android:centerColor="#D5EFFB"
                    android:centerY="1.0"
                    android:endColor="#D5EFFB"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>

</layer-list>